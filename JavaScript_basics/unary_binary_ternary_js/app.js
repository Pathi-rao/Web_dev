// Ternary operator

const value = 1 < 2

value ? console.log('Value is True') : console.log('Value is False')

// compact way of writing if else conditions

/* Fun fact: If you have loom extension enabled on chrome, it throws an error sometimes. 
We can fix it either updating or removing that extension */